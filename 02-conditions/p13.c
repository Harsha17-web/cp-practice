#include <stdio.h>
 int main () {
  int d,m,y:
  scanf("%d %d %d",&d,&m,&y);
 
 if( m<1 || m>12) {
  printf("invalid year\n"0;
 return0;
}
 if( y < 1 ) {
 printf("invalid year");
 return 0;
}
 if( m== 1 &
